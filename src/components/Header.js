import React from 'react';
import {Link} from '@reach/router'

const Header = () => {
    return (
        <div>
            <Link to="/home">Home</Link>
        </div>
    );
};

export default Header;